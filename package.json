{
  "name": "x-factory",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@11.16.0",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm --workspace @x-factory/web run dev",
    "build": "npm --workspace @x-factory/web run build",
    "lint": "npm --workspace @x-factory/web run lint",
    "typecheck": "npm --workspace @x-factory/web run typecheck",
    "test": "npm --workspace @x-factory/web run test"
  },
  "engines": {
    "node": ">=24 <25"
  }
}
